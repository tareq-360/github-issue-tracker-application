- 
    - 1️⃣ What is the difference between var, let, and const?
    - 
     ans: var= var has use old coding and it function scoped.
          let= let is changable its not fixed in veriable.
          const= const is most useable for veriable declairing time.
    - 
    - 2️⃣ What is the spread operator (...)?
    -  ans: spread operator is an powerful operation it can marge,copy,combine object and array.
    -  
    - 3️⃣ What is the difference between map(), filter(), and forEach()?
    -  ans: map() can traverse array and object and is has many powerful operation. filter can find the maching valuel.
    -  forEach() is an loop operaton it use for array type object iteration.
    -  
    - 4️⃣ What is an arrow function?
    - ans:  arrow  function is more useful and simple then normal function its can call automatic .
    - 5️⃣ What are template literals?
    - ans: template literal Use ${} to insert variables or expressions inside a string.
    - 


